import React from 'react';

const Header = () => {
    return (
        <header>
            <h1>Daily Task Manager</h1>
            <h2>Record what you don't wanna do --</h2>
            <h2> (but have to to) and clear them!</h2>
        </header>
    );
};

export default Header;